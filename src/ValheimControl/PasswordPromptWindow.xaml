<Window x:Class="ValheimControl.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm"
        Width="420" SizeToContent="Height"
        Background="{StaticResource BgDarkBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display">

    <Grid Margin="22">
        <StackPanel>
            <TextBlock x:Name="MessageText" TextWrapping="Wrap" Margin="0,0,0,14" />
            <PasswordBox x:Name="PasswordInput" KeyDown="PasswordInput_KeyDown" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
                <Button x:Name="CancelButton" Content="CANCEL" Style="{StaticResource GhostButtonStyle}"
                        Margin="0,0,10,0" Click="Cancel_Click" />
                <Button x:Name="OkButton" Content="CONFIRM" Style="{StaticResource PrimaryButtonStyle}"
                        Click="Ok_Click" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
